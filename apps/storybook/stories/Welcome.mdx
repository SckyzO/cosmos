import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Welcome" />

# Cosmos design system

Cosmos is the design system used across `sckyzo`'s projects (rackscope,
monitoring-hub, monitoring-hub-website). It ships as three npm packages:

- **`@sckyzo/cosmos-theme`** — Tailwind 4 theme + CSS variables (CSS only).
- **`@sckyzo/cosmos-react`** — React 19 component library.
- **`@sckyzo/cosmos-charts`** — opinionated [ApexCharts](https://apexcharts.com/) wrappers
  themed for Cosmos. Kept separate so consumers without charts don't ship the bundle.

## Sidebar layout

The components are grouped by **responsibility**, not by file location.
This mirrors the Storybook
[story-hierarchy convention](https://storybook.js.org/docs/writing-stories/naming-components-and-hierarchy)
(`Design System / Atoms / Button` etc.) and keeps the navigation flat enough to scan.

<table>
  <thead>
    <tr>
      <th align="left">Group</th>
      <th align="left">Contains</th>
    </tr>
  </thead>
  <tbody>
    <tr><td><strong>Pages</strong></td><td>Full-page templates — Dashboard, Auth pages, Settings, Blog (Index + Post), DocPage, HomeScreen, DetailScreen, ErrorPage…</td></tr>
    <tr><td><strong>Atoms</strong></td><td>Indivisible primitives — Button, Badge, Avatar, Spinner, IconBox, Link, ThemeToggle, BackToTopFab…</td></tr>
    <tr><td><strong>Forms</strong></td><td>Input controls — Input, Select, Combobox, Toggle, Checkbox, Radio, DatePicker, Dropzone, AvatarUploader, ActionPanel…</td></tr>
    <tr><td><strong>Data</strong></td><td>Data display — Card, KpiCard, CodeBlock, Timeline, MiniCalendar, DescriptionList, Accordion, Carousel, ImageGrid, VideoEmbed, IntegrationCard</td></tr>
    <tr><td><strong>Status</strong></td><td>Health indicators — StatusBadge, HealthDot, HealthBadge</td></tr>
    <tr><td><strong>Feedback</strong></td><td>Transient states — AlertBanner, NotificationCard, Toast, Tooltip, Skeleton, Loading/Empty/Error states, AnnouncementBar, CookieBanner</td></tr>
    <tr><td><strong>Overlays</strong></td><td>Floating UI — Modal, Drawer, Dropdown, Popover, CommandPalette, HelpOverlay, ConfirmationModal, NotificationsPanel, UserMenu</td></tr>
    <tr><td><strong>Navigation</strong></td><td>In-page nav — Tabs, Breadcrumb, PageBreadcrumb, Pagination, Stepper, ProgressBar, Navbar, VerticalNavigation, StickyScrollNav</td></tr>
    <tr><td><strong>Actions</strong></td><td>Contextual actions — PageActionButton, RefreshButton, RowActionButton, StatefulSaveButton, UnsavedIndicator</td></tr>
    <tr><td><strong>Layout</strong></td><td>Page skeletons + containers — Shell, Sidebar, Topbar, Card, Divider, MediaObject, ContentNarrow, ColBox, PageHeader, PageCard, SectionCard</td></tr>
    <tr><td><strong>Lists</strong></td><td>Repeatable rows + lists — StackedList family (Simple/Content/InCard/Narrow), ActivityFeed, GridList, SimpleRow, ClickableRow, StatusRow, List</td></tr>
    <tr><td><strong>Tables</strong></td><td>DataTable + sub-components</td></tr>
    <tr><td><strong>Settings</strong></td><td>Theme tools — AccentPicker, OptionPicker, PalettePicker</td></tr>
    <tr><td><strong>Charts</strong></td><td>BarChart + Gallery (`@sckyzo/cosmos-charts`)</td></tr>
  </tbody>
</table>

## Conventions

- Story IDs follow `Group/Component` (e.g. `Atoms/Button`, `Tables/DataTable`).
- Compound components expose sub-components via `Object.assign`
  (e.g. `<Modal><Modal.Header /><Modal.Body /><Modal.Footer /></Modal>`).
- Status colors are inlined per-component to satisfy Tailwind 4's source scanner.
- Dark mode is the default via `addon-themes` — toggle in the toolbar.

## Tooling

- Storybook 10 + Vite 7 (react renderer)
- `@storybook/addon-vitest` for component testing in browser mode (Playwright)
- `@storybook/addon-mcp` exposes a `/mcp` endpoint for AI agents
- All tooling runs in the `cosmos-dev` Docker container — host needs only Docker
